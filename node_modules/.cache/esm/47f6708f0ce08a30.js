const csv = _0ce‍.t("require")('csv-parser');
const fs = _0ce‍.t("require")('fs');


let Ytrain = [];
let Xtrain = []
let i=0;

fs.createReadStream('Final_pronta2.csv')
  .pipe(csv())
  .on('data', (row) => {
   

    Ytrain[i]=row.binnedAtraso;    
    delete row.binnedAtraso;
    Xtrain[i]=row;
    i++;
    
  })
  .on('end', () => {

   console.log(Xtrain.empresa.filter("TAM"));

  //  fastcsv
  //.write(data, { headers: true })
  //.pipe(ws);
  });






//var predictions = model.predict(Xtest);